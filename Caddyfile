:8080 {
    @unauth {
        path /*
    }
    @auth {
        header Authorization "Bearer {$CADDY_AUTHORIZATION}"
        path /*
    }
    route @auth {
        reverse_proxy {$CHROMA_RAILWAY_PRIVATE_DOMAIN}:8000
    }
    route @unauth {
        respond "Unauthorized" 401
    }
}